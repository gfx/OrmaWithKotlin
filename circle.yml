machine:
  java:
    version: oraclejdk8
  environment:
    TERM: dumb
dependencies:
  pre:
      - echo y | android -s update sdk -u -a -t "tools"
      - echo y | android -s update sdk -u -a -t "build-tools-24.0.3,android-24"
      - echo y | android -s update sdk -u -a -t "extra-android-m2repository"
test:
  override:
    - ./gradlew --info check
